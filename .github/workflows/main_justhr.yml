# # Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# # More GitHub Actions for Azure: https://github.com/Azure/actions

# name: Build and deploy WAR app to Azure Web App - JustHR

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read #This is required for actions/checkout

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Java version
#         uses: actions/setup-java@v4
#         with:
#           java-version: '21'
#           distribution: 'microsoft'

        
#       - name: Build with Maven
#         run: mvn clean package -DskipTests


#       - name: Upload artifact for deployment job
#         uses: actions/upload-artifact@v4
#         with:
#           name: java-app
#           path: '${{ github.workspace }}/target/*.jar'

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build 
#     permissions:
#       id-token: write #This is required for requesting the JWT
#       contents: read #This is required for actions/checkout

#     steps:
#       - name: Download artifact from build job
#         uses: actions/download-artifact@v4
#         with:
#           name: java-app
      
#       - name: Login to Azure
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_92E8CCE4D96943BE9657B12EF72D4497 }}
#           tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_455B7CA117C64C1F89AE84C707040C63 }}
#           subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_22B204B6745A4BDE9DEB01C94A01E729 }}

#       - name: List downloaded artifact files
#         run: |
#           echo "=== Workspace Root ==="
#           ls -la ${{ github.workspace }}
#           echo "=== Recursive Target Search ==="
#           find ${{ github.workspace }} -type f \( -name "*.jar" -o -name "*.war" -o -name "*.zip" \)
            
#       - name: Deploy to Azure Web App
#         id: deploy-to-webapp
#         uses: azure/webapps-deploy@v3
#         with:
#           app-name: 'JustHR'
#           slot-name: 'Production'
#           package: '${{ github.workspace }}/target/humanresource-0.0.1-SNAPSHOT.jar'








# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

# name: Build and deploy JAR app to Azure Web App - JustHR

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read #This is required for actions/checkout

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Java version
#         uses: actions/setup-java@v4
#         with:
#           java-version: '21'
#           distribution: 'microsoft'
        
#       - name: Build with Maven
#         run: mvn clean package -DskipTests

#       # Uploading only the specific jar file to avoid nesting issues
#       - name: Upload artifact for deployment job
#         uses: actions/upload-artifact@v4
#         with:
#           name: java-app
#           path: '${{ github.workspace }}/target/humanresource-0.0.1-SNAPSHOT.jar'

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build 
#     permissions:
#       id-token: write #This is required for requesting the JWT
#       contents: read #This is required for actions/checkout

#     steps:
#       - name: Download artifact from build job
#         uses: actions/download-artifact@v4
#         with:
#           name: java-app
#           path: ${{ github.workspace }} # Downloads the jar directly to workspace root
        
#       - name: Login to Azure
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_92E8CCE4D96943BE9657B12EF72D4497 }}
#           tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_455B7CA117C64C1F89AE84C707040C63 }}
#           subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_22B204B6745A4BDE9DEB01C94A01E729 }}
              
#       - name: Check downloaded files
#         run: |
#             pwd
#             ls -lah
#             file humanresource-0.0.1-SNAPSHOT.jar

#       - name: Inspect JAR
#         run: |
#             jar tf humanresource-0.0.1-SNAPSHOT.jar | head -20
      
#       - name: Deploy to Azure Web App
#         id: deploy-to-webapp
#         uses: azure/webapps-deploy@v3
#         with:
#           app-name: 'JustHR' # Corrected to your actual App Service resource name
#           slot-name: 'Production'
#           package: '${{ github.workspace }}/humanresource-0.0.1-SNAPSHOT.jar' # Simplified path
#           # justhr-cvd6fmbhfpc9dzdn










name: build-JustHumanResource
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Install glibc
        run: sudo apt-get update && sudo apt-get install -y libc6

  build-image:
    name: Build and push to docker
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Build and Push with Jib
        run: |
          mvn -B clean package jib:build -DskipTests \
            -Djib.to.image=kazeemakinrinde/justhumanresource:latest \
            -Djib.to.auth.username=${{ secrets.DOCKER_USERNAME }} \
            -Djib.to.auth.password=${{ secrets.DOCKER_PASSWORD }}

  deploy:
    name: Deploy to Azure App Service
    runs-on: ubuntu-latest
    needs: build-image
    steps:
      - name: Deploy Container to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'justHR2' # Replace with your exact Azure Web App name
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          images: 'kazeemakinrinde/justhumanresource:latest'
